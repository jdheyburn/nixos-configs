# Symlink to the host directory
host 

# If nixos-rebuild test was executed here, don't commit the output
result

# deploy-rs build cache for when --keep-result is defined
.deploy-gc

# nix-direnv cache
.direnv/

# Python bytecode cache (from update/ scripts)
__pycache__/

